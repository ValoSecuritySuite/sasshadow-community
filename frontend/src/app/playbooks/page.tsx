import { EnterprisePage } from "@/components/enterprise/enterprise-page";

export default function PlaybooksPage() {
  return (
    <EnterprisePage
      feature="playbooks"
      title="Playbooks"
      description="Response playbooks and the inbound action inbox."
    />
  );
}
